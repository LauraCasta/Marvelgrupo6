name: Upload Website

on: 
  push:
    branches:
      - main
    paths: 
      - 'comics/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@master
      - run:  npm install
        working-directory: ./comics
      - run:  CI='' npm run build
        working-directory: ./comics
      - uses: jakejarvis/s3-sync-action@master
        with: 
          args: --acl public-read --follow-symlinks --delete
        env: 
        
          AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET_COMICS}}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID_COMICS }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_COMICS }}
          AWS_REGION: 'us-east-1'
          SOURCE_DIR: './comics/build'